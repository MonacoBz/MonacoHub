spring:
  datasource:
    username: root
    password: Rocobz16
    url: jdbc:mysql://localhost:3306/foro_hub
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

